// This source code is subject to the terms of the Mozilla Public License 2.0 at https://mozilla.org/MPL/2.0/
// © goodgains


//@version=5
indicator("🔨 Toolbox Crypto-Futures Volume", overlay = false, precision = 0)

// @keywords: crypto-volume, exchange-selector, multi-exchange, binance, bybit, futures-volume, exchange-string, volume-toolbox, placeholder-plot, exchange-input, ftx, okx, bitget, prefix-label, crypto-futures, exchange-toggle, minimal-indicator, volume-pane, test-settings, close-plot

show_label_for_settings = input.bool(false, title = 'Show Exchange String', group = 'Test Settings', inline = '1')

if show_label_for_settings and barstate.islast
    label.new(bar_index + 10, close[1], text = syminfo.prefix)
    
use_exchange1 = input.bool(true, title = 'Exchange 1', group = 'Settings', inline = '1')
exhange1_string = input.string('BINANCE', title = '',  group = 'Settings', inline = '1', options = ['BINANCE', 'FTX', 'BYBIT', 'OKX', 'BITGET', 'PHEMEX', 'BITMEX', 'KRAKEN','BITFINEX'])

use_exchange2 = input.bool(true, title = 'Exchange 2', group = 'Settings', inline = '2')
exhange2_string = input.string('FTX', title = '',  group = 'Settings', inline = '2', options = ['BINANCE', 'FTX', 'BYBIT', 'OKX', 'BITGET', 'PHEMEX', 'BITMEX', 'KRAKEN','BITFINEX'])

use_exchange3 = input.bool(true, title = 'Exchange 3', group = 'Settings', inline = '3')
exhange3_string = input.string('BYBIT', title = '',  group = 'Settings', inline = '3', options = ['BINANCE', 'FTX', 'BYBIT', 'OKX', 'BITGET', 'PHEMEX', 'BITMEX', 'KRAKEN','BITFINEX'])

use_exchange4 = input.bool(true, title = 'Exchange 4', group = 'Settings', inline = '4')
exhange4_string = input.string('OKX', title = '',  group = 'Settings', inline = '4', options = ['BINANCE', 'FTX', 'BYBIT', 'OKX', 'BITGET', 'PHEMEX', 'BITMEX', 'KRAKEN','BITFINEX'])

use_exchange5 = input.bool(true, title = 'Exchange 5', group = 'Settings', inline = '5')
exhange5_string = input.string('BITGET', title = '',  group = 'Settings', inline = '5', options = ['BINANCE', 'FTX', 'BYBIT', 'OKX', 'BITGET', 'PHEMEX', 'BITMEX', 'KRAKEN','BITFINEX'])



plot(close)


